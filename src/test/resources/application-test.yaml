spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    sql: OFF
    org.hibernate.orm.jdbc.bind: OFF
    org.springframework.test.context.jdbc: OFF
    org.springframework.jdbc.datasource.init.jdbc: OFF
news:
  api:
    url: "https://newsapi.org/v2"
    api-key: ${NEWS_API_KEY}
app:
  scheduling:
    enable: false