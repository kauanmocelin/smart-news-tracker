spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    sql: debug
    org.hibernate.orm.jdbc.bind: trace
    org.springframework.test.context.jdbc: debug
    org.springframework.jdbc.datasource.init.jdbc: debug

